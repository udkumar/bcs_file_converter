    <div class="bs-example bs-example-tabs" role="tabpanel" data-example-id="togglable-tabs">
      <ul id="myTab" class="nav nav-tabs nav-tabs-responsive" role="tablist">
        <li role="presentation" class="active">
          <a href="#home" id="home-tab" role="tab" data-toggle="tab" aria-controls="home" aria-expanded="true">
            <span class="text">All Converted File</span>
          </a>
        </li>
        <li role="presentation" class="next">
          <a href="#profile" role="tab" id="profile-tab" data-toggle="tab" aria-controls="profile">
            <span class="text">Profile</span>
          </a>
        </li>
      </ul>

      <div id="myTabContent" class="tab-content">
        <div role="tabpanel" class="tab-pane fade in active" id="home" aria-labelledby="home-tab">
          <div class="row">
          <div class="col-sm-12 col-lg-6">
            <table class="table">
              <thead class="thead">
                <tr>
                  <th>#</th>
                  <th>File Name</th>
                </tr>
              </thead>

              <tbody class="tbody">
                <% @post_attachments.each do |post_attachment| %>
                  <tr>
                    <td><%= post_attachment.id %></td>
                    <td><%= File.basename(post_attachment.avatar.path) %></td>
                    <td><%= link_to 'Show', post_attachment %></td>
                    <td><%= link_to 'Destroy', post_attachment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
            <button type="button" class="btn btn-xs btn-default pull-right" data-toggle="modal" data-target="#myModal">Upload New File</button>
            <%= will_paginate @post_attachments, renderer: BootstrapPagination::Rails %>
          </div>
          <div class="col-sm-12 col-lg-6">

          </div>
        </div>
        </div>
        <div role="tabpanel" class="tab-pane fade" id="profile" aria-labelledby="profile-tab">
          <p>
            Will update soon.....
          </p>
        </div>

<!-- Modal -->
<div id="myModal" class="modal fade" role="dialog">
  <div class="modal-dialog">
    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Upload File</h4>
      </div>
      <div class="modal-body">
         <%= render 'form' %>
      </div>
    </div>

  </div>
</div>
